---
tags:
  - LeetCode
---

# [3370. Smallest Number With All Set Bits](https://leetcode.com/problems/smallest-number-with-all-set-bits/description/)  

+ 日期 : 2025/10/29  

## 問題描述  

給定一個數字 `n`，找出大於等於 `n` 的最小數，該數字的二進位表示中所有的位元都是 `1`。  

## 直覺想法  

1. 從 `0` 開始，如果小於 `n` 就將其乘 `2` 後加 `1`  

## 題解  

```cpp=
class Solution {
public:
    int smallestNumber(int n) {
        int res = 0;
        while(res < n) {
            res <<= 1;
            res |= 1;
        }
        return res;
    }
};
```

Time Complexity : $O(logn)$  
Space Complexity : $O(1)$  

## 心得  

連續好幾天簡單，很恐怖欸  
