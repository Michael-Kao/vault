---
tags:
  - LeetCode
---

# [1437. Check If All 1's Are at Least Length K Places Away](https://leetcode.com/problems/check-if-all-1s-are-at-least-length-k-places-away/description/)  

+ 日期 : 2025/11/17  

## 問題描述  

給定一個數字陣列 `nums`，和一個整數 `k`，計算陣列中數字 `1` 之間的距離是否至少為 `k`。  

## 直覺想法  

1. 紀錄前次 `1` 的位置，若遇到 `1`，則計算與前次 `1` 的距離是否小於 `k`，若是則回傳 `False`。  

## 題解  

```cpp=
class Solution {
public:
    bool kLengthApart(vector<int>& nums, int k) {
        int pre = -1e7;
        for(int i = 0; i < nums.size(); ++i) {
            if(nums[i] == 1) {
                if(i - pre - 1 < k)
                    return false;
                pre = i;
            }
        }
        return true;
    }
};
```

Time Complexity : $O(n)$  
Space Complexity : $O(1)$  

## 心得  

需要多一點這種題目  
