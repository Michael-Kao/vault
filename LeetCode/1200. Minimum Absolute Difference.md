---
tags:
  - LeetCode
---

# [1200. Minimum Absolute Difference](https://leetcode.com/problems/minimum-absolute-difference/submissions/1897483954/)  

+ 日期 : 2026/01/26  

## 問題描述  

## 直覺想法  

## 題解  

```cpp=
class Solution {
public:
    vector<vector<int>> minimumAbsDifference(vector<int>& arr) {
        sort(arr.begin(), arr.end());
        int target = INT_MAX;
        for(int i = 0; i < arr.size() - 1; ++i) {
            target = min(target, arr[i+1]-arr[i]);
        }
        vector<vector<int>> res;
        for(int i = 0; i < arr.size() - 1; ++i) {
            if(arr[i+1] - arr[i] == target) {
                res.push_back({arr[i], arr[i+1]});
            }
        }
        return res;
    }
};
```

Time Complexity : $O(nlogn)$  
Space Complexity : $O(n)$  

## 心得  

I keep worrying that I cannot finish the tasks on time.  
